name: clustering_hdbscan
task: clustering
dataset: iris
model: hdbscan
scaler: standard_scaler
reducer: pca
random_state: 42

cv:
  strategy: kfold
  n_splits: 3
  random_state: 42

optimization:
  enabled: true
  n_trials: 30
  direction: maximize
  study_name: clustering_hdbscan
  storage: sqlite:///clustering_optuna.db
  load_if_exists: true
  sampler: tpe
  pruner: null
