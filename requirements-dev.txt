# Development tooling — install with: pip install -r requirements-dev.txt
# (this pulls in requirements.txt automatically via the -e install).

-r requirements.txt

pytest>=7.4.0
pytest-cov>=4.1.0
ruff>=0.1.0
mypy>=1.5.0
pre-commit>=3.5.0
mkdocs>=1.5.0
mkdocs-material>=9.0.0
mkdocstrings[python]>=0.23.0
