# containeer_optuna settings (read by containeer_optuna.config.Settings).
# Copy to .env and adjust. All vars use the CO_ prefix.

CO_DIR=.
CO_DATA_DIR=data
CO_RESULTS_DIR=results
CO_LOG_LEVEL=INFO
CO_OPTUNA_STORAGE=sqlite:///optuna_studies.db
CO_CLUSTERING_STORAGE=sqlite:///clustering_optuna.db
